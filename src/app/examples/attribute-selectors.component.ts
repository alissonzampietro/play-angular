import { Component } from '@angular/core';

@Component({
  selector: '[app-attribute-selectors]',
  template:`
    <p>The attribute selectors is working: selector: '[app-attribute-selectors]'</p>
  `
})
export class AttributeSelectorsComponent {

}
